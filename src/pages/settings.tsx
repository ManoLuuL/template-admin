import Layout from "@/components/layout";

export default function Settings() {
  return (
    <Layout subTitle="Estrutura pagina Settings" title="Pagina Settings">
      <h4>Settings</h4>
    </Layout>
  );
}
